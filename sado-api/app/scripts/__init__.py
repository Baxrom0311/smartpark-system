"""Operational scripts that ship with the API.

Modules in this package are intended to be invoked as
``python -m app.scripts.<name>``. They share the application's settings
and database engine so behaviour matches a running API instance.
"""
